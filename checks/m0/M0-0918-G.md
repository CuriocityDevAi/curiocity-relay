---
id: M0-0918-G
hub: m0
pr:
  hotfix: https://github.com/CuriocityDevAi/storeport/pull/123
  spike: https://github.com/CuriocityDevAi/storeport/pull/124
  fork: https://github.com/CuriocityDevAi/agilo-medusa-pos-fork/pull/7
issued_at: 2026-09-18T20:00:00Z
author: orchestrator
items:
  - '{"device":"curl","title":"K1 착지 조건 · curl /health = {ok:true, db.ok:true}","ok":"응답 200 · {ok:true, db:{ok:true}}","ng":"200 아님 · db.ok false · 크래시 재현","est_min":1}'
  - '{"device":"web","title":"K2 Railway Deployments · 최근 배포 SUCCESS","ok":"배지 초록 · 로그 [file] R2 미설정 · 로컬 저장","ng":"FAILED · Access key ID required 재현","est_min":2}'
  - '{"device":"expo","title":"K3 Login (fork PR #7 병합 후) · Shop URL 검증 15s · cold-start hint 표시","ok":"URL 통과 · Sign In 진입 · 필드 아래 hint 노출","ng":"5s 만에 fail · hint 미표시","est_min":2}'
  - '{"device":"web","title":"K4 Admin 이미지 업로드 · R2 env 미설정이므로 file-local 저장","ok":"업로드 200 · 이미지 URL = local path","ng":"업로드 500 · 크래시","est_min":2}'
---

## 요지

M0-0918-G · **긴급 핫픽스**: R2 provider env guard 로 프로덕션 크래시 루프 해소.

**착지 조건 (Kyu 정본)**: Railway 배포 초록 + `/health` 200 실측.

**핵심 회귀**:
- **K1 · K2**: PR #123 병합 후 Railway 자동 배포 성공 확진. 이전 M0-0918-D 배포는 "Access key ID … required" 로 크래시 루프.
- **K3**: fork PR #7 병합 후 Login URL 검증 timeout 15s 여유 · Railway cold-start 대응.
- **K4**: R2 env 미설정 상태 = file-local 폴백 (부팅 로그 확진).

사전:
- storeport PR #123 (긴급 핫픽스) 병합.
- fork PR #6 + #7 병합 (별건 · K3 은 이후).
- Kyu Cloudflare 계정 세팅 = **별건** (본 라운드 밖).

## R2 Kyu 클릭 3줄 (반복 게시 · 향후 라운드)

1. Cloudflare Dashboard → R2 → Create bucket (`storeport-images` · APAC · Public).
2. R2 → API Tokens → Create (Read+Write · bucket scope) → copy keys+endpoint.
3. Railway → storeport-production → Variables → 5개 env 편입 → Deploy SUCCESS.

## Kyu 도장 필요 (본 라운드 완결 판정)

- PR #123 병합 → Railway 자동 배포 SUCCESS + /health 200 확진.
- PR #124 (docs) 병합.
- fork PR #6 + #7 병합.
- 이후 R2 실 세팅 · 속도 실측 · Singapore 이전 판정 = 별건.
